# Hands-On 7 - Angular Student Portal

Run `npm install` then `npm start`. The standalone implementation demonstrates components, `ngModel` search, `HttpClient` through `CourseService`, routing, and a validated reactive profile form.
