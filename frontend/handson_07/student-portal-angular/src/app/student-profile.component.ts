import { Component } from '@angular/core'; import { ReactiveFormsModule,FormControl,FormGroup,Validators } from '@angular/forms'; import { NgIf } from '@angular/common';
@Component({standalone:true,imports:[ReactiveFormsModule,NgIf],template:`<h1>Student profile</h1><form [formGroup]="profileForm" (ngSubmit)="submit()"><label>Name <input formControlName="name"></label><small *ngIf="profileForm.controls.name.touched&&profileForm.controls.name.invalid">Name is required</small><label>Email <input formControlName="email"></label><small *ngIf="profileForm.controls.email.touched&&profileForm.controls.email.invalid">Enter a valid email</small><label>Semester <input type="number" formControlName="semester"></label><button [disabled]="profileForm.invalid">Save profile</button></form>`,styles:[`form,label{display:grid;gap:.4rem;max-width:360px;margin:.8rem 0}small{color:#b42318}button{padding:.6rem}`]}) export class StudentProfileComponent{profileForm=new FormGroup({name:new FormControl('',{nonNullable:true,validators:Validators.required}),email:new FormControl('',{nonNullable:true,validators:[Validators.required,Validators.email]}),semester:new FormControl(1,{nonNullable:true,validators:[Validators.required,Validators.min(1),Validators.max(8)]})});submit(){console.log(this.profileForm.value)}}
