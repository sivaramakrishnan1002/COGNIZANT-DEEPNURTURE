import { Component,OnInit } from '@angular/core'; import { FormsModule } from '@angular/forms'; import { NgFor,NgIf } from '@angular/common'; import { CourseService } from './course.service'; import { CourseCardComponent } from './course-card.component';
@Component({standalone:true,imports:[FormsModule,NgFor,NgIf,CourseCardComponent],template:`<h1>My courses</h1><label>Search courses <input [(ngModel)]="searchTerm" placeholder="Search courses"></label><p *ngIf="loading">Loading courses…</p><section *ngIf="!loading"><app-course-card *ngFor="let c of filteredCourses;trackBy:trackById" [name]="c.name" [code]="c.code" [credits]="c.credits" [grade]="c.grade"/><p *ngIf="!filteredCourses.length">No courses found</p></section>`,styles:[`section{display:grid;grid-template-columns:repeat(auto-fit,minmax(210px,1fr));gap:1rem}input{margin:.6rem;padding:.5rem}`]}) export class CourseListComponent implements OnInit{searchTerm='';loading=true;courses:any[]=[];constructor(private courseService:CourseService){}ngOnInit(){this.courseService.getCourses().subscribe({next:posts=>{this.courses=posts.map((p,i)=>({id:p.id,name:p.title,code:'CS'+(101+i),credits:i%2?3:4,grade:'Pending'}));this.loading=false},error:()=>{this.loading=false}})}get filteredCourses(){return this.courses.filter(c=>c.name.toLowerCase().includes(this.searchTerm.toLowerCase()))}trackById=(_:number,c:any)=>c.id;}
