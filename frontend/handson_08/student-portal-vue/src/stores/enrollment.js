import {defineStore} from 'pinia';import {computed,ref} from 'vue';import {courses} from '../courses';
export const useEnrollmentStore=defineStore('enrollment',()=>{const enrolledCourses=ref([]);const totalCredits=computed(()=>enrolledCourses.value.reduce((sum,c)=>sum+c.credits,0));function enroll(course){if(!enrolledCourses.value.some(c=>c.id===course.id))enrolledCourses.value.push(course)}function unenroll(id){enrolledCourses.value=enrolledCourses.value.filter(c=>c.id!==id)}async function fetchAndEnroll(id){await Promise.resolve();const course=courses.find(c=>c.id===Number(id));if(course)enroll(course)}function $reset(){enrolledCourses.value=[]}return{enrolledCourses,totalCredits,enroll,unenroll,fetchAndEnroll,$reset}});
